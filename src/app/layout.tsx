import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Saran - Portfolio",
  description: "Full Stack Developer & Software Engineer. Passionate about creating innovative web applications and solving complex problems.",
  keywords: ["developer", "portfolio", "web development", "software engineer", "react", "nextjs", "saran"],
  authors: [{ name: "Saran" }],
  openGraph: {
    title: "Saran - Portfolio",
    description: "Full Stack Developer & Software Engineer",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
